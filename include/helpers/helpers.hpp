#ifndef APTRACER_HELPERS_HELPERS_HPP
#define APTRACER_HELPERS_HELPERS_HPP
namespace APTracer {
    /**
     * @brief Contains helper functions and types used through the library.
     *
     * Helpers are not directly used to compute images, but make some aspects of the program easier.
     */
    namespace Helpers {
    }
}

#include "Colours.hpp"
#include "InputParser_t.hpp"

#endif
